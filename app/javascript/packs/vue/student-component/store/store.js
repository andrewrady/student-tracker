import Vue from 'vue';
import Vuex from 'vuex';
import axios from 'axios';

Vue.use(Vuex);

export const state = {
    //states
}

export const mutation = {
    //changes to state
}

export const actions = {
    //api calls
}